<section class="section section-limestone" id="book">
  <div class="container cta-block">
    <h2 class="cta-heading">One call. A clear picture of what your customers could stop worrying about.</h2>
    <p class="lead cta-sub">
      30 minutes. We'll show you exactly which operational problems disappear first,
      how fast your customers see the difference, and what the revenue impact looks like — even if you never work with us.
    </p>

    <div class="cta-stats">
      <div class="cta-stat">
        <span class="stat-number cta-stat-value">30 min</span>
        <span class="cta-stat-label">Assessment call</span>
      </div>
      <div class="cta-divider"></div>
      <div class="cta-stat">
        <span class="stat-number cta-stat-value">2-4 weeks</span>
        <span class="cta-stat-label">First results visible</span>
      </div>
      <div class="cta-divider"></div>
      <div class="cta-stat">
        <span class="stat-number cta-stat-value">0</span>
        <span class="cta-stat-label">Data leaving your servers</span>
      </div>
    </div>

    <a href="#book" class="btn btn-primary">Book your free assessment &rarr;</a>
  </div>
</section>

<style>
  .cta-block {
    text-align: center;
    max-width: 720px;
  }

  .cta-heading {
    font-size: 36px;
    margin-bottom: 16px;
  }

  .cta-sub {
    margin-bottom: 40px;
  }

  .cta-stats {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 32px;
    margin-bottom: 40px;
  }

  .cta-stat {
    display: flex;
    flex-direction: column;
    gap: 4px;
  }

  .cta-stat-value {
    font-size: 16px;
    color: var(--navy);
  }

  .cta-stat-label {
    font-family: var(--font-mono);
    font-size: 11px;
    color: var(--sandstone);
  }

  .cta-divider {
    width: 1px;
    height: 32px;
    background: var(--sandstone);
  }

  @media (max-width: 768px) {
    .cta-heading {
      font-size: 28px;
    }
    .cta-stats {
      flex-direction: column;
      gap: 16px;
    }
    .cta-divider {
      width: 40px;
      height: 1px;
    }
  }
</style>
